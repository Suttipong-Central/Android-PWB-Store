<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/margin_padding_small">

    <TextView
        android:id="@+id/tvFilterCategory"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textColor="@color/nameProductColor"
        app:layout_constraintEnd_toEndOf="@+id/tvFilterTopicCount"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Category Name" />

    <TextView
        android:id="@+id/tvFilterTopicCount"
        style="@style/TextAppearance.AppCompat.Caption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_padding_small"
        app:layout_constraintBottom_toBottomOf="@+id/tvFilterCategory"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1"
        app:layout_constraintStart_toEndOf="@+id/tvFilterCategory"
        app:layout_constraintTop_toTopOf="@+id/tvFilterCategory"
        tools:text="(99)" />

</androidx.constraintlayout.widget.ConstraintLayout>